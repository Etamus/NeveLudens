<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="NeveLudens"
        Width="980"
        Height="600"
        MinWidth="980"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="Transparent"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                             ResizeBorderThickness="6"
                             GlassFrameThickness="0"
                             CornerRadius="18"
                             UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#000000"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#151515"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#171717"/>
        <SolidColorBrush x:Key="SurfaceRaisedBrush" Color="#202020"/>
        <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#292929"/>
        <SolidColorBrush x:Key="InputBrush" Color="#0D0D0D"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#303030"/>
        <SolidColorBrush x:Key="BorderStrongBrush" Color="#484848"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F3F5F7"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#A9B1BC"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#8b8b8b"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#F1F3F5"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#DDE1E6"/>
        <SolidColorBrush x:Key="AccentTextBrush" Color="#111317"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#55C894"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#E1B45B"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#EC7272"/>
        <SolidColorBrush x:Key="DangerSurfaceBrush" Color="#202020"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="PageTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="PageSubtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,7"/>
        </Style>

        <Style x:Key="CaptionText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
        </Style>

        <Style x:Key="Section" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="MinWidth" Value="112"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.38"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.38"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource DangerSurfaceBrush}"/>
            <Setter Property="Foreground" Value="#FFD7D7"/>
            <Setter Property="BorderBrush" Value="#613338"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.38"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StopButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="Opacity" Value="0.78"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseWindowButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="0,16,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavigationButton" TargetType="Button">
            <Setter Property="Height" Value="46"/>
            <Setter Property="Margin" Value="10,3"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Foreground" Value="#B8B8B8"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavBorder" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="active">
                                <Setter TargetName="NavBorder" Property="Background" Value="{DynamicResource SurfaceRaisedBrush}"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="11,7"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="TextBorder" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="TextBorder" Property="Opacity" Value="0.48"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.48"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="3,1">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#303030"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="11,0"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MaxDropDownHeight" Value="260"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemStyle}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="DropDownToggle"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder"
                                                Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="34"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Margin="11,0,6,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Stretch"
                                                                  TextElement.Foreground="{DynamicResource TextPrimaryBrush}"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplateSelector="{Binding ItemTemplateSelector, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
                                                <Path Grid.Column="1"
                                                      Width="10"
                                                      Height="6"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Stroke="{DynamicResource TextSecondaryBrush}"
                                                      StrokeThickness="1.6"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      Data="M 1 1 L 5 5 L 9 1"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=DropDownToggle}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Width="{Binding ActualWidth, ElementName=DropDownToggle}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Margin="0,4,0,0"
                                        Padding="3"
                                        Background="{DynamicResource SurfaceRaisedBrush}"
                                        BorderBrush="{DynamicResource BorderStrongBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="DropDownToggle" Property="Opacity" Value="0.48"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Unchecked">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SwitchThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.16">
                                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="SwitchOn" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.16"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SwitchThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="18" Duration="0:0:0.16">
                                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="SwitchOn" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.16"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Indeterminate"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Width="42" Height="24" Margin="16,0,0,0" CornerRadius="12" Background="#282828" ClipToBounds="True">
                                <Grid>
                                    <Border x:Name="SwitchOn" Background="#555555" CornerRadius="12" Opacity="0"/>
                                    <Ellipse x:Name="SwitchThumb" Width="18" Height="18" Margin="3" HorizontalAlignment="Left" Fill="#FFFFFF">
                                        <Ellipse.RenderTransform><TranslateTransform X="0"/></Ellipse.RenderTransform>
                                    </Ellipse>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusPill" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>

        <Style x:Key="SettingDivider" TargetType="Border">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Margin" Value="0,14"/>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="MinWidth" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Background="Transparent" BorderThickness="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Margin="2,1" Background="#4A4A4A" CornerRadius="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Background="Transparent" BorderThickness="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="1" BorderBrush="{DynamicResource BorderStrongBrush}" BorderThickness="1" CornerRadius="18" Background="Transparent">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border x:Name="TitleBar" Grid.Row="0" Background="{DynamicResource SidebarBrush}" CornerRadius="17,17,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="BtnMinimize" Style="{StaticResource WindowButton}" ToolTip="Minimizar">
                            <Path Width="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" Data="M 0 5 L 10 5"/>
                        </Button>
                        <Button x:Name="BtnMaximize" Style="{StaticResource WindowButton}" ToolTip="Maximizar">
                            <Path Width="10" Height="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.2" Data="M 0.6 0.6 L 9.4 0.6 L 9.4 9.4 L 0.6 9.4 Z"/>
                        </Button>
                        <Button x:Name="BtnClose" Style="{StaticResource CloseWindowButton}" ToolTip="Fechar">
                            <Path Width="10" Height="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" Data="M 1 1 L 9 9 M 9 1 L 1 9"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="210"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource SidebarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" CornerRadius="0,0,0,17">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Margin="20,26,16,24">
                            <TextBlock FontSize="21" FontWeight="Bold" Text="NeveLudens"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1">
                            <Button x:Name="NavOverview" Style="{StaticResource NavigationButton}" CommandParameter="overview" Tag="active" AutomationProperties.Name="Início">
                                <StackPanel Orientation="Horizontal">
                                    <Viewbox Width="21" Height="21">
                                        <Path Width="24" Height="24" Fill="Transparent" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M 3 10.5 L 12 3 L 21 10.5 M 5 9.5 L 5 20 L 10 20 L 10 14 L 14 14 L 14 20 L 19 20 L 19 9.5"/>
                                    </Viewbox>
                                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="Início"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="NavSettings" Style="{StaticResource NavigationButton}" CommandParameter="settings" AutomationProperties.Name="Configurações">
                                <StackPanel Orientation="Horizontal">
                                    <Viewbox Width="21" Height="21">
                                        <Path Width="24" Height="24" Fill="Transparent" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M 14.7 6.3 A 1 1 0 0 0 14.7 7.7 L 16.3 9.3 A 1 1 0 0 0 17.7 9.3 L 21.47 5.53 A 6 6 0 0 1 13.53 13.47 L 6.62 20.38 A 2.12 2.12 0 0 1 3.62 17.38 L 10.53 10.47 A 6 6 0 0 1 18.47 2.53 Z"/>
                                    </Viewbox>
                                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="Configurações"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="NavDiagnostics" Style="{StaticResource NavigationButton}" CommandParameter="diagnostics" AutomationProperties.Name="Logs">
                                <StackPanel Orientation="Horizontal">
                                    <Viewbox Width="21" Height="21">
                                        <Path Width="24" Height="24" Fill="Transparent" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M 3 4 L 21 4 L 21 20 L 3 20 Z M 7 8 L 11 12 L 7 16 M 13 16 L 17 16"/>
                                    </Viewbox>
                                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="Logs"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Background="{DynamicResource BackgroundBrush}" CornerRadius="0,0,17,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                    <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,1" Background="{DynamicResource BackgroundBrush}">
                        <Grid Margin="28,0,28,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="SessionGameText" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Text="Nenhum jogo selecionado"/>
                            <Button x:Name="StartButton" Grid.Column="1" Width="88" MinWidth="88" Height="36" Padding="14,9" FontSize="15" VerticalAlignment="Center" Style="{StaticResource PrimaryButton}" IsEnabled="False">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Path x:Name="StartButtonPowerIcon"
                                          Width="14"
                                          Height="14"
                                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                          StrokeThickness="1.6"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Stretch="Uniform"
                                          Data="M 12 2 L 12 11 M 6.35 5.35 A 8 8 0 1 0 17.65 5.35"/>
                                    <TextBlock x:Name="StartButtonLabel"
                                               Margin="5,0,0,0"
                                               VerticalAlignment="Center"
                                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                               Text="Iniciar"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>

                    <Border x:Name="NotificationBar" Grid.Row="1" Margin="28,14,28,0" Padding="14,11" CornerRadius="6" BorderThickness="1" Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="NotificationTitle" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock x:Name="NotificationDetail" Margin="0,3,0,0" FontSize="12" TextWrapping="Wrap"/>
                            </StackPanel>
                            <Button x:Name="NotificationDetailsButton" Grid.Column="1" Margin="16,0,0,0" Style="{StaticResource TextButton}" Content="Ver detalhes"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid x:Name="OverviewPage">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Margin="32,26,32,18">
                                <TextBlock Style="{StaticResource PageTitle}" Text="Início"/>
                            </StackPanel>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="32,0,32,30">
                                    <Border Style="{StaticResource Section}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="18"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Style="{StaticResource SectionTitle}" FontWeight="Bold" Text="Processo"/>
                                                </StackPanel>
                                                <Button x:Name="RefreshButton" Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="Atualizar"/>
                                            </Grid>
                                            <Grid Grid.Row="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1.35*"/>
                                                    <ColumnDefinition Width="16"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Style="{StaticResource FieldLabel}" Foreground="{DynamicResource TextMutedBrush}" Text="Janela detectada"/>
                                                    <ComboBox x:Name="ProcessCombo" Height="46">
                                                        <ComboBox.ItemTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding Display}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FullDisplay}"/>
                                                            </DataTemplate>
                                                        </ComboBox.ItemTemplate>
                                                    </ComboBox>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Style="{StaticResource FieldLabel}" Foreground="{DynamicResource TextMutedBrush}" Text="Processo do jogo (.exe)"/>
                                                    <TextBox x:Name="ManualProcessBox"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <Border Margin="0,16,0,0" Style="{StaticResource Section}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="24"/>
                                                <ColumnDefinition Width="1.45*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel>
                                                <TextBlock Style="{StaticResource SectionTitle}" FontWeight="Bold" Margin="0,0,0,32" Text="Predefinições"/>
                                                <TextBlock Style="{StaticResource FieldLabel}" Foreground="{DynamicResource TextMutedBrush}" Text="Configuração"/>
                                                <ComboBox x:Name="PresetCombo" Height="42">
                                                    <ComboBoxItem Content="Padrão" Tag="default" IsSelected="True"/>
                                                    <ComboBoxItem Content="Desempenho" Tag="performance"/>
                                                    <ComboBoxItem Content="Qualidade" Tag="quality"/>
                                                    <ComboBoxItem Content="Personalizada" Tag="custom" Visibility="Collapsed" IsHitTestVisible="False"/>
                                                </ComboBox>
                                                <TextBlock x:Name="PresetDescriptionText"
                                                           Margin="8,12,0,0"
                                                           Style="{StaticResource CaptionText}"
                                                           TextWrapping="Wrap"/>
                                            </StackPanel>

                                            <Border Grid.Column="2"
                                                    Height="188"
                                                    Background="#080808"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="7"
                                                    ClipToBounds="True">
                                                <Grid>
                                                    <Image x:Name="LastCaptureImage"
                                                           Stretch="Uniform"
                                                           RenderOptions.BitmapScalingMode="HighQuality"
                                                           Visibility="Collapsed"/>
                                                    <TextBlock x:Name="LastCapturePlaceholder"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               Foreground="{DynamicResource TextMutedBrush}"
                                                               FontSize="12"
                                                               Text="Disponível na saída detalhada."/>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </ScrollViewer>
                        </Grid>

                        <Grid x:Name="SettingsPage" Visibility="Collapsed">
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <StackPanel Margin="32,26,32,18">
                                <TextBlock Style="{StaticResource PageTitle}" Text="Configurações"/>
                            </StackPanel>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="32,0,32,32">
                                    <Border Style="{StaticResource Section}">
                                        <StackPanel>
                                            <TextBlock Style="{StaticResource SectionTitle}" FontWeight="Bold" Margin="0,0,0,26" Text="Inferência"/>

                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Modelo"/>
                                                    <TextBlock Margin="0,3,20,0" Style="{StaticResource CaptionText}" Text="Ajusta as camadas de funcionamento do modelo." TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <ComboBox x:Name="ModelCombo" Grid.Column="1">
                                                    <ComboBoxItem Content="Padrão" Tag="default" IsSelected="True"/>
                                                    <ComboBoxItem Content="Dinâmico" Tag="pizza_tower"/>
                                                    <ComboBoxItem Content="Acelerado" Tag="pizza_tower_fast"/>
                                                </ComboBox>
                                            </Grid>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Método de captura"/>
                                                    <TextBlock Margin="0,3,20,0" Style="{StaticResource CaptionText}" Text="Ferramenta usada para a captura de tela." TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <ComboBox x:Name="BackendCombo" Grid.Column="1">
                                                    <ComboBoxItem Content="Automático" Tag="auto" IsSelected="True"/>
                                                    <ComboBoxItem Content="DXcam" Tag="dxcam"/>
                                                    <ComboBoxItem Content="PyAutoGUI" Tag="pyautogui"/>
                                                </ComboBox>
                                            </Grid>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Modo de captura"/>
                                                    <TextBlock Margin="0,3,20,0" Style="{StaticResource CaptionText}" Text="Controla o uso do recurso de xspeedhack." TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <ComboBox x:Name="RuntimeModeCombo" Grid.Column="1">
                                                    <ComboBoxItem Content="Precisão" Tag="precision" IsSelected="True"/>
                                                    <ComboBoxItem Content="Tempo real" Tag="realtime"/>
                                                </ComboBox>
                                            </Grid>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Saída de depuração"/>
                                                    <TextBlock Margin="0,3,20,0" Style="{StaticResource CaptionText}" Text="Armazenamento de imagens, vídeos e artefatos de sessão." TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <ComboBox x:Name="OutputModeCombo" Grid.Column="1">
                                                    <ComboBoxItem Content="Simples" Tag="normal" IsSelected="True"/>
                                                    <ComboBoxItem Content="Detalhado" Tag="debug"/>
                                                </ComboBox>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <Border Margin="0,16,0,0" Style="{StaticResource Section}">
                                        <StackPanel>
                                            <TextBlock Style="{StaticResource SectionTitle}" FontWeight="Bold" Margin="0,0,0,26" Text="Comportamento"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Modo de jogo"/>
                                                    <TextBlock Margin="0,3,20,0" Style="{StaticResource CaptionText}" Text="Define a persepção visual do modelo." TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <ComboBox x:Name="GameModeCombo" Grid.Column="1">
                                                    <ComboBoxItem Content="Padrão" Tag="default" IsSelected="True"/>
                                                    <ComboBoxItem Content="Luta" Tag="fighting"/>
                                                    <ComboBoxItem Content="Tela dividida" Tag="split_screen"/>
                                                </ComboBox>
                                            </Grid>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Modo de jogador"/>
                                                    <TextBlock Margin="0,3,20,0" Style="{StaticResource CaptionText}" Text="Seleciona o fluxo de entrada do controle virtual." TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <ComboBox x:Name="AgentSlotCombo" Grid.Column="1">
                                                    <ComboBoxItem Content="Padrão" Tag="auto" IsSelected="True"/>
                                                    <ComboBoxItem Content="Jogador 2" Tag="player2"/>
                                                    <ComboBoxItem Content="Jogador 2 (Co-op)" Tag="player2_coop"/>
                                                </ComboBox>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <Border Margin="0,16,0,0" Style="{StaticResource Section}">
                                        <StackPanel>
                                            <TextBlock Style="{StaticResource SectionTitle}" FontWeight="Bold" Margin="0,0,0,26" Text="Recursos avançados"/>
                                            <CheckBox x:Name="MultimodalSupervisorCheck" Style="{StaticResource ToggleSwitch}" IsChecked="False">
                                                <StackPanel>
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Supervisor multimodal"/>
                                                    <TextBlock Margin="0,3,24,0" Style="{StaticResource CaptionText}" Text="Usa um segundo modelo para orientar nas ações."/>
                                                </StackPanel>
                                            </CheckBox>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <CheckBox x:Name="AdvancedMemoryCheck" Style="{StaticResource ToggleSwitch}" IsChecked="False">
                                                <StackPanel>
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Memória avançada"/>
                                                    <TextBlock Margin="0,3,24,0" Style="{StaticResource CaptionText}" Text="Mantém memória visual, lugares e resultados para a VLM."/>
                                                </StackPanel>
                                            </CheckBox>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <CheckBox x:Name="SmartRecoveryCheck" Style="{StaticResource ToggleSwitch}" IsChecked="True">
                                                <StackPanel>
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Recuperação inteligente"/>
                                                    <TextBlock Margin="0,3,24,0" Style="{StaticResource CaptionText}" Text="Habilita memória temporal e recuperação anti-loop."/>
                                                </StackPanel>
                                            </CheckBox>
                                            <Border Style="{StaticResource SettingDivider}"/>
                                            <CheckBox x:Name="MenuActionsCheck" Style="{StaticResource ToggleSwitch}" IsChecked="False">
                                                <StackPanel>
                                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="Permitir acesso aos menus"/>
                                                    <TextBlock Margin="0,3,24,0" Style="{StaticResource CaptionText}" Text="Libera as ações START, BACK e GUIDE."/>
                                                </StackPanel>
                                            </CheckBox>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>

                        <Grid x:Name="DiagnosticsPage" Visibility="Collapsed">
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <Grid Margin="32,26,32,18">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Style="{StaticResource PageTitle}" Text="Logs"/>
                                </StackPanel>
                                <Button x:Name="DiagnoseButton" Grid.Column="1" VerticalAlignment="Center" Style="{StaticResource SecondaryButton}" Content="Diagnosticar"/>
                            </Grid>
                            <Grid Grid.Row="1" Margin="32,0,32,30">
                                <Border Style="{StaticResource Section}" Padding="0">
                                    <Grid>
                                        <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16,0">
                                            <Grid>
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                <TextBlock VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Text="Saída da execução"/>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Button x:Name="CopyLogButton" Style="{StaticResource TextButton}" Content="Copiar"/>
                                                    <Button x:Name="ClearLogButton" Margin="4,0,0,0" Style="{StaticResource TextButton}" Content="Limpar"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                        <RichTextBox x:Name="LogBox" Grid.Row="1" Padding="16,13" Background="#000000" BorderThickness="0" Foreground="#C9D0D8" FontFamily="Cascadia Mono, Consolas" FontSize="11.5" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                            <FlowDocument PagePadding="0"/>
                                        </RichTextBox>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

        </Grid>
    </Border>
</Window>
